#include "main.h"
/**
 * isupper - function that checks for uppercase character.
 *@c: input number.
 * return: 1 if upper, 0 if else.
 */

int _isupper(int c);
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
