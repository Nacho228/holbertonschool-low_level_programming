#include "main.h"
#include <string.h>

/**
 * _puts - prints a string.
 * @str: characters str.
 * Return: 0.
 */

int _puts(char *str)
{
char s = {'s', 't', 'r', 'i', 'n', 'g', '\0'};
_putchar(s);
{
return (0);
}
}
